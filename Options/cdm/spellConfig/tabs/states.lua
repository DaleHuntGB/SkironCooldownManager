local SCM = select(2, ...)
local Options = SCM.Options
local CDMOptions = Options.CDM

function CDMOptions.CreateStateTabSettings(iconSettingsTabs, iconSettings, scrollFrame, buttonFrame, buttonData, buttonConfig, anchorIndex, mode, isGlobal, isBuffBar)
end